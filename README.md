# Tinkercad-mqtt-bridge
A two-way bridge between Tinkercad Serial Monitor and MQTT brokers using Node.js and Puppeteer.

# Requirements: 
- Windows (OS)
- Google Chrome installed

# Run this in your project folder:
npm install mqtt puppeteer
